import { useActivity } from './useActivity';
import { useAppointments } from './useAppointments';
import { useAssessment } from './useAssessment';
import { useAuth } from './useAuth';
import { useClaims } from './useClaims';
import { useCoverage } from './useCoverage';
import { useCycle } from './useCycle';
import { useDevices } from './useDevices';
import { useGamification } from './useGamification';
import { useHealthMetrics } from './useHealthMetrics';
import { useJourney } from './useJourney';
import { useMedicalRecords } from './useMedicalRecords';
import { useMedications } from './useMedications';
import { useNotifications } from './useNotifications';
import { useNutrition } from './useNutrition';
import { usePayments } from './usePayments';
import { usePlan } from './usePlan';
import { useProfile } from './useProfile';
import { useSearch } from './useSearch';
import { useSettings } from './useSettings';
import { useSleep } from './useSleep';
import { useSymptomChecker } from './useSymptomChecker';
import { useTelemedicine } from './useTelemedicine';
import { useVisits } from './useVisits';
import { useWellness } from './useWellness';

/**
 * Exports the hook for fetching activity-type health metrics.
 * @see useActivity
 */
export { useActivity };

/**
 * Exports the hook for managing appointments in the Care Now journey.
 * @see useAppointments
 */
export { useAppointments };

/**
 * Exports the hook for fetching health assessment data.
 * @see useAssessment
 */
export { useAssessment };

/**
 * Exports the hook for authentication functionality.
 * @see useAuth
 */
export { useAuth };

/**
 * Exports the hook for managing insurance claims in the My Plan journey.
 * @see useClaims
 */
export { useClaims };

/**
 * Exports the hook for accessing insurance coverage information in the My Plan journey.
 */
export { useCoverage };

/**
 * Exports the hook for fetching cycle tracking health metrics.
 * @see useCycle
 */
export { useCycle };

/**
 * Exports the hook for managing connected health devices in the My Health journey.
 * @see useDevices
 */
export { useDevices };

/**
 * Exports the hook for accessing gamification features across all journeys.
 * @see useGamification
 */
export { useGamification };

/**
 * Exports the hook for fetching and managing health metrics in the My Health journey.
 * @see useHealthMetrics
 */
export { useHealthMetrics };

/**
 * Exports the hook for managing the current journey context.
 * @see useJourney
 */
export { useJourney };

/**
 * Exports the hook for managing medical records data in the Care Now journey.
 * @see useMedicalRecords
 */
export { useMedicalRecords };

/**
 * Exports the hook for fetching and managing medications in the My Health journey.
 * @see useMedications
 */
export { useMedications };

/**
 * Exports the hook for managing user notifications across all journeys.
 * @see useNotifications
 */
export { useNotifications };

/**
 * Exports the hook for fetching nutrition-type health metrics.
 * @see useNutrition
 */
export { useNutrition };

/**
 * Exports the hook for managing payment data and actions in the Care Now journey.
 * @see usePayments
 */
export { usePayments };

/**
 * Exports the hook for managing the user's insurance plan, benefits, and digital card
 * in the 'My Plan & Benefits' journey.
 * @see usePlan
 */
export { usePlan };

/**
 * Exports the hook for fetching and managing the current user's profile data.
 * @see useProfile
 */
export { useProfile };

/**
 * Exports the hook for managing search state and fetching search results.
 * @see useSearch
 */
export { useSearch };

/**
 * Exports the hook for managing user settings including personal info,
 * dependents, addresses, preferences, and insurance documents.
 * @see useSettings
 */
export { useSettings };

/**
 * Exports the hook for fetching sleep-type health metrics.
 * @see useSleep
 */
export { useSleep };

/**
 * Exports the hook for managing the symptom checker flow in the Care Now journey.
 * @see useSymptomChecker
 */
export { useSymptomChecker };

/**
 * Exports the hook for managing telemedicine sessions in the Care Now journey.
 * @see useTelemedicine
 */
export { useTelemedicine };

/**
 * Exports the hook for managing visit data in the Care Now journey.
 * @see useVisits
 */
export { useVisits };

/**
 * Exports the hook for managing AI Wellness Companion data and actions
 * in the wellness journey (Module 06), including chat, mood, sessions,
 * daily plan, insights, goals, journal, challenges, streaks, and tips.
 * @see useWellness
 */
export { useWellness };
