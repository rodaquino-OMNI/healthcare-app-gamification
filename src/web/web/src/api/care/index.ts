/**
 * Care API Barrel
 *
 * Re-exports all care domain modules so that existing imports
 * like `from './care'` or `from '../care'` continue to resolve.
 */

export * from './care-appointments';
export * from './care-providers';
export * from './care-telemedicine';
