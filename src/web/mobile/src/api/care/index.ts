/**
 * Care API barrel - re-exports everything so that existing imports
 * like `from '../api/care'` or `from '@api/care'` continue to work.
 */
export * from './care-appointments';
export * from './care-providers';
export * from './care-telemedicine';
