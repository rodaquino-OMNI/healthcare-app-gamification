/**
 * Input component barrel file
 *
 * This file re-exports the Input component and its associated types
 * to provide a clean import path for consumers of the design system.
 *
 * @example
 * // Import the component and its types
 * import Input, { InputProps } from 'design-system/components/Input';
 */

export { default } from './Input';
export { default as Input } from './Input';
export type { InputProps } from './Input';
