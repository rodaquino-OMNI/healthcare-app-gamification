/**
 * Declaration file to provide the Journey enum from shared module
 */

declare module '@app/shared/constants/journey.constants' {
  export enum Journey {
    HEALTH = 'health',
    CARE = 'care',
    PLAN = 'plan'
  }
}