/**
 * Error type enum for notification service
 */
export enum ErrorType {
    VALIDATION = 'VALIDATION',
    BUSINESS = 'BUSINESS',
    TECHNICAL = 'TECHNICAL',
    EXTERNAL = 'EXTERNAL',
    NOT_FOUND = 'NOT_FOUND',
    UNAUTHORIZED = 'UNAUTHORIZED',
    FORBIDDEN = 'FORBIDDEN',
}
