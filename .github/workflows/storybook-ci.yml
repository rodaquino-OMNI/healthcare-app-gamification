name: Storybook CI

on:
  pull_request:
    paths:
      - 'src/web/design-system/**'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-storybook:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6
        with:
          node-version: '20'
          cache: 'yarn'
          cache-dependency-path: src/web/yarn.lock
      - name: Install dependencies
        working-directory: src/web
        run: yarn install --frozen-lockfile
      - name: Build Storybook
        run: yarn build-storybook
        working-directory: src/web/design-system
      - name: Upload Storybook artifact
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7
        with:
          name: storybook-build
          path: src/web/design-system/storybook-static
          retention-days: 7

  chromatic:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 0
      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6
        with:
          node-version: '20'
          cache: 'yarn'
          cache-dependency-path: src/web/yarn.lock
      - name: Install dependencies
        working-directory: src/web
        run: yarn install --frozen-lockfile
      - name: Publish to Chromatic
        uses: chromaui/action@5ec258af08deb3e8c36653bd618cb7fe52090031 # v15
        with:
          projectToken: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}
          workingDir: src/web/design-system
          exitZeroOnChanges: true
          exitOnceUploaded: true
